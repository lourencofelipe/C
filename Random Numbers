#include <stdio.h>
#include <stdlib.h>
#include <time.h>
 
//random numbers

int main(void){
    int i;
    system("color 2");
    
    printf("intervalo da rand: [0,%d]\n", RAND_MAX);
      for(i=1 ; i <= 120 ; i++) 
        printf("Number %d: %d\n",i, rand());
}
